import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'amplify-new-ui',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
